{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for RecursiveUI windows",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "opener:default",
    "shell:default",
    "core:event:default",
    "core:event:allow-listen",
    "core:event:allow-emit"
  ]
}
